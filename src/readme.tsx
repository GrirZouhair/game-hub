/*
    Block:
        -take full width if no width
        -add line break
        -respect padding, margin, width, height
    Inline:
        -do not respect width height
        -respect padding and margin [just left and right]
        -do not add line before
        -allow elements before and after it
    Inline-Block:
        - allow elements before and after it
        -respect padding, margin, width, height
*/